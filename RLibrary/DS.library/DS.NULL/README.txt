Folder '.\DS.NULL' is in place to allow FUDGE to run without a DS method. 

The DS algorithm will simply multiply the future predictor by 1.0 to predict the future DS values.

This method only allows a single predictor variable, which must be the same as the target var.

 